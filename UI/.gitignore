node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Wrangler local dev secrets (never commit)
.dev.vars
.dev.vars.*
!.dev.vars.example

# SQL files (schema dumps / migrations with potentially sensitive data)
*.sql

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
